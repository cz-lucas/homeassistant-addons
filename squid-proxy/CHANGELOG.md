# Changelog

## 1.0.0

- Initial release
- Based on Alpine Linux
- Squid proxy with IPv4 and IPv6 support
- Host network mode
- Configurable path to `squid.conf` in the share folder
- Default configuration generated on first start if none exists
